<% include ../partials/header %>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Price</th>
            <th><button type="button" class="btn btn-info" > Delete </button></th>
        </tr>
    </thead>
    <tbody>
        <% products.forEach(function(product){%>
        <tr>
            <td scope="row"><%= product.id %></td>
            <td><%= product.title %></td>
            <td><%= product.price %></td>
            <td><a name="" id="" class="btn btn-info" href="/products/<%= product.id %>" role="button">EDIT</a>
            <button type="button" class="btn btn-danger" > Delete </button></td>
        </tr>
        <% }); %>
        
    </tbody>
</table>

  <% include ../partials/footer %>